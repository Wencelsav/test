#include "stdafx.h"
#include <iostream>

#include "sstream"



using namespace std;



int main() {
	int  s[n],n;



	for (string string; getline(cin, string); ) {

		istringstream stream(string);

		bool failure = false;

		for (int i = 0; i < 10; ++i) {
			int j = ++i
				If(s[j] < s[i]) {
				cout << 'error' << endl;
			}


		}



	}
	return;
	
	}
void exp(char* str)
{
	int i = 0;
	int j = 0;
	int a, b, c, d;
		c=a+b;

	for (string string; getline(cin, string); ) {

		istringstream stream(string);
		while ((*(str + i) = *(str + j++)) != 'b') {}

		while ((*(str + i) = *(str + j++)) != 'a') {}
		
		if (d == c) {
			cout << a, b << endl;
		}
		else cout << error << endl;
	}
}
